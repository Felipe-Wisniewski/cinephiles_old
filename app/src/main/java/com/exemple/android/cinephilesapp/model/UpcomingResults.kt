package com.exemple.android.cinephilesapp.model

class UpcomingResults(val results: List<UpcomingMovie>,
                      val page: Int,
                      val total_results: Int,
                      val total_pages: Int)